{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "machina-cli",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "machina-plugin": "workspace:*",
    "machina-shared": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  }
}
