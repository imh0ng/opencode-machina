{
  "name": "@opencode-machina/machina-web",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "echo 'No build step for raw html/css' && exit 0",
    "test": "bun test tests/server.test.ts",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/bun": "1.3.8",
    "playwright": "^1.40.0",
    "typescript": "5.7.3"
  }
}
